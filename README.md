# CMG Group Bleeds Project - UW Madison

Create Compatible Environment by running the following command:

```python
conda env create -f environment_cmg.yml
```
And then, activate:
```python
conda activate <env_name>
```


